# 复制为 apps/web/.env.local（可选）。未设置时使用下列默认值。
# 文档：docs/DEVELOPMENT.md；后端变量见 apps/api/.env.example

# 后端 API 基址（浏览器直连，须与 apps/api 监听地址一致）
# 默认 http://127.0.0.1:8000
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# 前端页面（无额外环境变量）：
#   /library  — 文献库：扫描、批量索引/摘要、任务队列概览（SSE 进度）、展开详情（解析报告 / AI 摘要）
#   /chat     — 文献问答（流式、[n] 引用）
#   /review   — 综述 / 申请书（优先使用 paper_summary）
#   /settings — 展示后端生效路径；Zotero PDF 目录可在此保存（写入 apps/api/.env）
